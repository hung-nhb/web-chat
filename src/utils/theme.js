import { createTheme } from "@mui/material";

const theme = createTheme({
    lightMode: {
        background: {
            main: "#f0f2f5",
        },
        text: {
            main: "black",
            orther: "white",
        },
    },
});

export default theme;